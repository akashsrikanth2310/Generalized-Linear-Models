The r code to execute is nammed glm.r 

You can execute it directly, if it is throwing error we need to install the packages corresponding to it.

The code will display the summary of fit.all,fit.reducde,fit.single models and also the values of anova and overdispersion test.